Step 1 — Model
	•	Create models/Expense.js
	•	Fields: id, title, amount, category, date (default = today)
	•	Methods:
	•	get info() → formatted string
	•	Maybe a static parse(obj) like you did for Task

⸻

Step 2 — API
	•	Create api/expenseApi.js
	•	Functions:
	•	getAllExpenses()
	•	saveExpenses(expenses)
	•	addExpense(expense)
	•	removeExpense(id)
	•	updateExpense(id, updates)

(same pattern as taskApi.js, but for expenses.json)

⸻

Step 3 — CLI Helpers
	•	Create cli/expenseCliHelper.js
	•	Functions:
	•	addExpenseCli(idGen)
	•	listExpensesCli()
	•	updateExpenseCli()
	•	removeExpenseCli()

⸻

Step 4 — Data
	•	Create empty data/expenses.json

⸻

Step 5 — Integration
	•	In main.js add submenu for Expenses
	•	1. Manage Tasks
	•	2. Manage Expenses
	•	3. Exit
